# Historical engineering notes

The repository intentionally does not publish deployment history, customer topology, site names, equipment inventories, or generated runtime artifacts. Those records belong in a private operational system.

Use the canonical documents in `docs/` for the public project contract. New design notes must contain only generic behavior, synthetic examples, and references to public source files.
